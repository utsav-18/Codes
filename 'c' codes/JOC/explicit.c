//Explicit type conversion, also called type casting;

// => instead of being done automatically according to the rules of the language for implicit type conversion.
// It is requested by the user in the program.

#include <stdio.h>
int main () {

double x = 10.7;

int value = (int)x; 

 printf("%d\n", value);

    return 0;

}

