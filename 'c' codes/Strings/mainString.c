#include <stdio.h>
#include <string.h>

int main() {

char str1[] = "I AM IRON MAN !!\n";
char str2[] = "I AM GROOT !!";

//printf("%s",str1);
puts(str1);
//printf("%s",str2);
puts(str2);

return 0;
}