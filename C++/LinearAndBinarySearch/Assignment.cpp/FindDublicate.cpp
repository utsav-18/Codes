#include <iostream>
#include <vector>
using namespace std;
  
  int find(vector<int> &nums){
        int lo=0;
        int hi=nums.size()-1;

        while(lo<=hi){
            int mid=lo+(hi-lo)/2;

            //not completed
        }
      
  }
  
    int main() {
        vector<int> v;
        v={1,2,2,3,4,5,6,7,8};
        cout<<find(v);
    }

